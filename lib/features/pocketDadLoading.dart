import 'package:flutter/material.dart';

class PocketDadLoading extends StatelessWidget {
  const PocketDadLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
